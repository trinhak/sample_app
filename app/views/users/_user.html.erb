<li>
  <%= gravatar_for user, size: Settings.user.user_size %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t(".delete"), user, method: :delete,
      data: { confirm: t(".delete_mess") } %>
  <% end %>
</li>
